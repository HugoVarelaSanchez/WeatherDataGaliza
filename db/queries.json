{
	"Schema": {
		"Tables": "SELECT name FROM sqlite_master WHERE type='table'"
	},
	"WObservation": {
		"Empty": "DELETE FROM WeatherObservation",
		"Get": "SELECT sky_state, temperature, wind, precipitation_amount, relative_humidity, air_pressure_at_sea_level FROM WeatherObservation WHERE date = ? AND time = ?"
	}
}
